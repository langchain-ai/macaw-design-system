/**
 * ID of the synthetic segment that `otherCollationThreshold` collapses small
 * segments into. Consumers can match on it to special-case the slice in a
 * tooltip or to seed `selectedIds`.
 */
export const DONUT_OTHER_SEGMENT_ID = 'donut-other';

export const DONUT_OTHER_SEGMENT_LABEL = 'Other';
